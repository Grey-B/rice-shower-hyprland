The posh themes should be in `$HOME/.poshthemes/` after installing oh-my-posh.

This setup utilizes the `montys.omp.json` theme, but feel free to copy the colors into a theme of your preference, or skip this if you don't want to use oh-my-posh.
